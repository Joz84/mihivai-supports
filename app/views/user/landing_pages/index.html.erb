<div class="d-md-flex flex-wrap">
  <div class="container-fluid px-5 py-3" style="width:60%;background:rgb(0,175,160);min-height:91vh;color:white !important;" id="new_landing_page">
    <%= render "form", landing_page: @landing_page, url: user_landing_pages_path %>
  </div>

  <div class="container-fluid text-center" style="width:40%;overflow:auto;display:block;min-height:91vh" id="display_landing_pages">
    <h1 class="display-4 mt-5 mb-5">Mes pages</h1>
    <div class="mt-5 d-md-flex flex-wrap justify-content-center text-center align-item-center mx-auto" style="width:22rem">
      <% current_user.landing_pages.each do |page| %>
        <div class="d-flex justify-content-center flex-column">
          <%= render "landing_pages/landing_page", page: page %>
          <div class="d-flex flex-wrap justify-content-center align-item-center">
            <% page.users.each do |user| %>
              <div class="mt-2 mb-3">
                <%= user.first_name %>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
